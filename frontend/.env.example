# Адрес API-сервера.
# В dev — пусто (Vite проксирует /api на localhost:3001).
# В production — домен портала, например https://portal.vekpmt.ru
VITE_API_URL=

# reCAPTCHA (пока не используется, оставь пустым)
VITE_RECAPTCHA_SITE_KEY=
